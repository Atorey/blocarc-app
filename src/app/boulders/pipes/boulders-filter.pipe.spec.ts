import { BouldersFilterPipe } from './boulders-filter.pipe';

describe('BouldersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BouldersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
