import { AchievementsFilterPipe } from './achievements-filter.pipe';

describe('BouldersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AchievementsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
