import { GradesFilterPipe } from './grades-filter.pipe';

describe('GradesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GradesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
