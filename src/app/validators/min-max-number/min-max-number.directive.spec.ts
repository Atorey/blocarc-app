import { MinMaxNumberDirective } from './min-max-number.directive';

describe('MinMaxNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MinMaxNumberDirective();
    expect(directive).toBeTruthy();
  });
});
